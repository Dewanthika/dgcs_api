enum OrderStatusEnum {
  PENDING = 'pending',
  SHIPPED = 'shipped',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  PAID = 'paid',
  COMPLETE = 'complete',
}

export default OrderStatusEnum;
